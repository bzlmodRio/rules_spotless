package com.diffplug.bazel.spotless;

import com.diffplug.bazel.spotless.config.GenericConfig;
import com.diffplug.bazel.spotless.config.YamlConfig;

public class YamlFormatter extends BaseFormatter {
	public YamlFormatter(GenericConfig genericConfig, YamlConfig config) {
		super(genericConfig);

		throw new UnsupportedOperationException();
	}
}
