package com.diffplug.bazel.spotless;

import com.diffplug.bazel.spotless.config.GenericConfig;
import com.diffplug.bazel.spotless.config.JsonConfig;

public class JsonFormatter extends BaseFormatter {
	public JsonFormatter(GenericConfig genericConfig, JsonConfig config) {
		super(genericConfig);

		throw new UnsupportedOperationException();
	}
}
